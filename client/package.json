{
  "name": "ubuntu_remote_desctop",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite --port=8000 --host=0.0.0.0",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^4.5.4",
    "vite": "^2.8.0"
  },
  "dependencies": {
    "axios": "^0.26.1"
  }
}
